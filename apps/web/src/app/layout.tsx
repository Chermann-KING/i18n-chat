/**
 * Root layout shell — required by Next.js App Router.
 * The actual `<html>` and `<body>` are rendered by the locale-scoped layout
 * at `app/[locale]/layout.tsx`.
 */
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
