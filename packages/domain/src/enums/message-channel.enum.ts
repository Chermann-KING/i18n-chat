/**
 * Supported message delivery channels.
 * Used throughout the application to identify which channel a message is sent through.
 */
export enum MessageChannel {
  EMAIL = 'EMAIL',
  SMS = 'SMS',
  WHATSAPP = 'WHATSAPP',
}
